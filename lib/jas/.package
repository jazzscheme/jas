(package jas
  
  (title "Jas")
  (description "Jas client and server")
  (authors "Guillaume Cartier")
  (stage alpha)
  (root "src")
  (products (jas-all (dependencies jazz.associative
                                   jazz.debuggee
				   jazz.event
                                   jazz.io
				   jazz.network
				   jazz.platform
				   jazz.process
				   jazz.settings
				   jazz.syntax
				   jazz.time
				   jazz.version
				   jazz.zlib
				   srfi-19
                                   jas
                                   jas-server))))
